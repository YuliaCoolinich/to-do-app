import { Checkbox } from '@mui/material/'
import { styled } from '@mui/material/styles'
import { deepPurple } from '@mui/material/colors'

export default styled(Checkbox)(() => ({
  // 'accentColor': deepPurple[700],
  'color': deepPurple[700],
}))
